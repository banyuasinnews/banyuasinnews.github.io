function esc(s){return String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]));}
function toggleSearch(){const p=document.getElementById("searchPanel");p.style.display=p.style.display==="block"?"none":"block";if(p.style.display==="block")document.getElementById("searchInput").focus();}
function card(n){return `<article class="card"><div class="card-photo">${n.image?`<img src="${esc(n.image)}" alt="${esc(n.title)}">`:"FOTO"}</div><div class="card-body"><span class="tag">${esc(n.category)}</span><h3><a href="artikel-${esc(n.id)}.html">${esc(n.title)}</a></h3><small>${esc(n.date)} • ${esc(n.author)}</small></div></article>`}
function side(n){return `<article><div class="side-photo">${n.image?`<img src="${esc(n.image)}" alt="${esc(n.title)}">`:"FOTO"}</div><span class="tag">${esc(n.category)}</span><h3><a href="artikel-${esc(n.id)}.html">${esc(n.title)}</a></h3><small>${esc(n.date)}</small></article>`}
function render(){if(typeof NEWS==="undefined"||!NEWS.length)return;const n=NEWS[0];document.getElementById("lead").innerHTML=`<div class="lead-photo">${n.image?`<img src="${esc(n.image)}" alt="${esc(n.title)}">`:"FOTO BERITA UTAMA"}</div><div class="lead-copy"><span class="tag">${esc(n.category)}</span><h1><a href="artikel-${esc(n.id)}.html">${esc(n.title)}</a></h1><p>${esc(n.excerpt)}</p><div class="by">${esc(n.date)} <b>•</b> ${esc(n.author)}</div></div>`;document.getElementById("featured").innerHTML=NEWS.slice(1,3).map(side).join("")||side(n);document.getElementById("latest").innerHTML=NEWS.map(card).join("");document.getElementById("popular").innerHTML=NEWS.slice(0,5).map((x,i)=>`<li><a href="artikel-${esc(x.id)}.html">${esc(x.title)}</a><small>${esc(x.category)}</small></li>`).join("");}
function searchNews(){const q=document.getElementById("searchInput").value.toLowerCase().trim(),r=document.getElementById("results");if(!q){r.innerHTML="";return}const f=NEWS.filter(n=>(n.title+" "+n.category+" "+n.excerpt).toLowerCase().includes(q));r.innerHTML=f.length?f.map(n=>`<p><a href="artikel-${esc(n.id)}.html">${esc(n.title)}</a></p>`).join(""):"<p>Tidak ditemukan.</p>";}
document.addEventListener("DOMContentLoaded",render);